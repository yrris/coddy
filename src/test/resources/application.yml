spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:coddy;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false
  session:
    store-type: none
